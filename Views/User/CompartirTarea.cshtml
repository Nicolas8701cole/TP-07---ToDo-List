<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>

<body>
<!--Falta hacer, muestra las opciones a editar-->
<!--y te permite escrivir el nombre de la persona a la que se lo mandaras-->
    <main>
        <h2>Escrive a quien se lo deseas compartir:</h2>
        <h2>Que tarea compartiras?</h2>
        @foreach (Tareas tarea in ViewBag.dato)
        {
            <form asp-controller="User" asp-action="MandarAEditarTarea" method="post">
                <p>

                <div>
                    @if (@tarea.estado != 1)
                    {
                        <input type="radio" name="idTarea" value="@tarea.id" />
                        <label>@tarea.nombre</label>
                        <input type="submit" value="Marcar como finalizada" />
                    }
                </div>

                </p>
            </form>
        }
    </main>
    <a asp-controller="User" asp-action="AdministrarMovimientosEntrePestañas" asp-route-eleccion="7">
        Volver a decisiones
    </a>


</body>

</html>