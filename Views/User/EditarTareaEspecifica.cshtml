<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pene, el que mira es gey</title>
</head>
@{
    var tarea = ViewBag.tarea as Tareas;
}
<body>
    <main>
    <h2>Editar Tarea: </h2>
            <form asp-controller="User" asp-action="GuardarEdicionDeTarea" method="post" autocomplete="false" tabindex="-1">
            <p>
                Opciones <br><!--Radio button-->
                <label for="nombreTarea">@tarea.nombre</label>
                <input type="text" id="nombreTarea" name="nombreTarea" required>

                <label for="descripcionTarea">@tarea.descripcion</label>
                <input type="text" id="descripcionTarea" name="descripcionTarea" required>

                <label for="fecha">Fecha</label>
                <input type="date" id="fecha" name="fecha" value='@tarea.fecha.ToString("yyyy-MM-dd")' required />

                @if(@tarea.estado == 0){
                <label for="estadoTarea">Por hacer</label>
                <select name="estadoTarea">
                <option value="0">Por hacer</option>
                <option value="2">En proceso</option>
                <option value="1">Hecho</option>
                </select>
                }else if(@tarea.estado == 1){
                <label for="estadoTarea">Hecho</label>
                <select name="estadoTarea">
                <option value="0">Por hacer</option>
                <option value="2">En proceso</option>
                <option value="1">Hecho</option>
                </select>
                }else{
                <label for="estadoTarea">En proceso</label>
                <select name="estadoTarea">
                <option value="0">Por hacer</option>
                <option value="2">En proceso</option>
                <option value="1">Hecho</option>
                </select>
                }
                <input type="hidden" name="id" value=@tarea.id />
                <input type="submit" value="Ingresar Respuesta">
            </p>
            </form>
            <a asp-controller="User" asp-action="AdministrarMovimientosEntrePestañas" asp-route-eleccion="7">
                Volver a decisiones
            </a>
    </main>
</body>

</html>